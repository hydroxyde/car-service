// export all our components so we can import them in other pages

import Hero from './Hero';
import CustomButton from './CustomButton';
import Navbar from './Navbar';
import Footer from './Footer';

export {
    Hero,
    CustomButton,
    Navbar,
    Footer,
}